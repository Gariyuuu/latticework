---
slug: functions
title: Functions
estimatedMinutes: 14
blocks:
  - id: intro
    type: explanation
  - id: try-it
    type: codeExample
    language: python
  - id: args
    type: explanation
  - id: exercise-1
    type: exercise
    exerciseType: write-code
    testCasesId: functions-ex1
    difficulty: beginner
    concept: python-functions
    prompt: "Write a function `max_of_three(a, b, c)` that returns the largest of the three arguments."
  - id: exercise-2
    type: exercise
    exerciseType: write-code
    testCasesId: functions-ex2
    difficulty: intermediate
    concept: python-functions
    prompt: "Write a function `apply_twice(f, x)` that calls the function f on x, then calls f again on the result, and returns that final value. (This is your first taste of higher-order functions — functions that take other functions as arguments.)"
  - id: quiz-1
    type: quiz
    concept: python-functions
    question: "What does a Python function return if it has no explicit `return` statement?"
    options: ["None", "0", "Raises an error", "An empty string"]
    answer: 0
  - id: checkpoint-1
    type: checkpoint
---

<Explanation id="intro">

A function is defined with `def`, a name, and parentheses holding its
parameters. `return` sends a value back to the caller — without it, the
function returns `None`.

```python
def greet(name):
    return f"Hello, {name}!"

print(greet("World"))  # Hello, World!
```

</Explanation>

<CodeExample id="try-it" language="python" code={`def greet(name):\n    return f"Hello, {name}!"\n\nprint(greet("World"))`} caption="f-strings (starting with f before the quote) are the standard way to interpolate variables into text." />

<Explanation id="args">

Parameters can have default values, and can be passed by keyword:

```python
def greet(name, greeting="Hello"):
    return f"{greeting}, {name}!"

greet("Ada")                    # "Hello, Ada!"
greet("Ada", greeting="Hi")     # "Hi, Ada!"
```

Functions are also values in Python — you can pass one function as an
argument to another. That's the idea behind the next exercise.

</Explanation>

<Exercise id="exercise-1" />

<Exercise id="exercise-2" />

<Quiz id="quiz-1" />

<Checkpoint id="checkpoint-1">
You can define functions with parameters, default arguments, and pass functions as values.
</Checkpoint>
