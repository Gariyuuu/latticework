---
slug: exceptions
title: Exceptions
estimatedMinutes: 14
blocks:
  - id: intro
    type: explanation
  - id: try-it
    type: codeExample
    language: python
  - id: specific
    type: explanation
  - id: exercise-1
    type: exercise
    exerciseType: write-code
    testCasesId: exceptions-ex1
    difficulty: beginner
    concept: python-exceptions
    prompt: "Write a function `safe_divide(a, b)` that returns `a / b`, or the string 'undefined' if `b` is 0 — catch the specific exception `/` raises, don't check `b == 0` beforehand."
  - id: exercise-2
    type: exercise
    exerciseType: write-code
    testCasesId: exceptions-ex2
    difficulty: intermediate
    concept: python-exceptions
    prompt: "Write a function `parse_int_list(strings)` that converts each string in `strings` to an int, skipping any that fail to convert, and returns the successfully-converted ints in order."
  - id: quiz-1
    type: quiz
    concept: python-exceptions
    question: "What's the risk of a bare `except:` (with no exception type) compared to `except Exception:`?"
    options: ["It also silently catches things like KeyboardInterrupt and SystemExit, which you almost never want to swallow", "There's no real difference between them", "Bare except runs noticeably faster", "except Exception: only exists in Python 2"]
    answer: 0
  - id: checkpoint-1
    type: checkpoint
---

<Explanation id="intro">

When something goes wrong, Python raises an **exception** — normally that
crashes your program with a traceback. `try`/`except` lets you catch it
and decide what to do instead of crashing.

```python
try:
    result = 10 / 0
except ZeroDivisionError:
    result = None
    print("can't divide by zero")
```

Only wrap the specific line(s) that might fail — putting an entire large
function inside one `try` block makes it hard to tell which line actually
raised, when something goes wrong later.

</Explanation>

<CodeExample id="try-it" language="python" code={`def get_first(items):\n    try:\n        return items[0]\n    except IndexError:\n        return None\n\nprint(get_first([1, 2, 3]))  # 1\nprint(get_first([]))          # None — no crash`} caption="Catching a specific exception type (IndexError) means a genuinely different bug elsewhere in the function won't get silently swallowed by an overly broad except." />

<Explanation id="specific">

Always catch the narrowest exception type that matches what you expect to
go wrong — `ValueError` for a bad conversion, `KeyError` for a missing
dict key, `ZeroDivisionError` for division, `IndexError` for an
out-of-range index. A bare `except:` (or `except Exception:`, which is
only slightly narrower) catches *everything*, including bugs you didn't
anticipate — that turns a loud crash (which tells you something's wrong)
into a silent wrong answer, which is much harder to debug later.

```python
def to_int(s):
    try:
        return int(s)
    except ValueError:
        return None
```

</Explanation>

<Exercise id="exercise-1" />

<Exercise id="exercise-2" />

<Quiz id="quiz-1" />

<Checkpoint id="checkpoint-1">
You can catch specific exception types with try/except and know why overly broad exception handling is a real bug risk, not just a style preference.
</Checkpoint>
