{
  "id": "cross-validation-ex2",
  "type": "write-code",
  "language": "python",
  "starterCode": "def kfold_split_indices(n, k):\n    fold_size = n // k\n    folds = []\n    for i in range(k):\n        start = i * fold_size\n        end = start + fold_size if i < k - 1 else n\n        folds.append(list(range(start, end)))\n    return folds\n\ndef cross_val_scores(y_true, y_pred, k):\n    ...\n",
  "cases": [
    { "call": "cross_val_scores([1, 0, 1, 0, 1, 0], [1, 0, 0, 0, 1, 1], 3)", "expect": "[1.0, 0.5, 0.5]" }
  ]
}
