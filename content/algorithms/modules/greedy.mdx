---
slug: greedy
title: Greedy Algorithms
estimatedMinutes: 15
blocks:
  - id: intro
    type: explanation
  - id: try-it
    type: codeExample
    language: python
  - id: risk
    type: explanation
  - id: exercise-1
    type: exercise
    exerciseType: write-code
    testCasesId: greedy-ex1
    difficulty: beginner
    concept: greedy
    prompt: "Write a function `max_activities(intervals)` that returns the maximum number of non-overlapping intervals selectable from `intervals` (a list of `(start, end)` tuples). Use the greedy 'earliest end time first' strategy."
  - id: exercise-2
    type: exercise
    exerciseType: write-code
    testCasesId: greedy-ex2
    difficulty: intermediate
    concept: greedy
    prompt: "Write a function `min_coins_greedy(coins, amount)` that returns the list of coins used to make `amount`, greedily taking the largest coin from `coins` (given sorted descending) that still fits, repeatedly."
  - id: quiz-1
    type: quiz
    concept: greedy
    question: "What is the key risk of a greedy algorithm compared to dynamic programming?"
    options: ["It makes locally optimal choices that aren't always globally optimal", "It's always slower than DP", "It can't be implemented recursively", "It always requires more memory"]
    answer: 0
  - id: checkpoint-1
    type: checkpoint
---

<Explanation id="intro">

A **greedy** algorithm builds a solution by always making the choice that
looks best *right now*, never reconsidering it later. That's cheap — often
O(n log n), usually just a sort plus one pass — but only correct for
problems where the locally-best choice is provably part of some globally-
best solution.

**Activity selection** is the textbook example where greedy works: given a
set of intervals, pick the maximum number that don't overlap. Sorting by
*end time* and always taking the next interval that starts after the last
one you picked ends is provably optimal.

```python
def max_activities(intervals):
    intervals = sorted(intervals, key=lambda x: x[1])
    count = 0
    last_end = float("-inf")
    for start, end in intervals:
        if start >= last_end:
            count += 1
            last_end = end
    return count
```

</Explanation>

<CodeExample id="try-it" language="python" code={`print(max_activities([(1, 3), (2, 4), (3, 5)]))\n# 2 — pick (1,3) then (3,5); (2,4) overlaps both`} caption="Sorting by end time (not start time!) is the key insight — it always leaves the most room for whatever comes next." />

<Explanation id="risk">

Greedy doesn't always give the optimal answer — that's the trade-off for
its speed. Making change with US coins (`[25, 10, 5, 1]`) happens to work
greedily because of how those denominations are structured, but with coins
like `[1, 3, 4]` for amount `6`, greedy picks `4 + 1 + 1` (3 coins) while
the true optimum is `3 + 3` (2 coins). When you're not sure greedy is
correct for a problem, dynamic programming (previous module) is the safe,
always-correct fallback — at the cost of more time and memory.

</Explanation>

<Exercise id="exercise-1" />

<Exercise id="exercise-2" />

<Quiz id="quiz-1" />

<Checkpoint id="checkpoint-1">
You can implement a greedy algorithm for interval scheduling and coin-making, and know when greedy can silently give a wrong answer.
</Checkpoint>
