---
slug: scaling
title: Scaling
estimatedMinutes: 18
blocks:
  - id: intro
    type: explanation
  - id: try-it
    type: codeExample
    language: python
    packages: ["pandas"]
  - id: standardization
    type: explanation
  - id: exercise-1
    type: exercise
    exerciseType: write-code
    testCasesId: scaling-ex1
    difficulty: beginner
    concept: feature-engineering-scaling
    prompt: "Write `min_max_scale(series)`: rescale a pandas Series to the range [0, 1] via `(x - min) / (max - min)`. Return a list of values, each rounded to 4 decimals."
  - id: exercise-2
    type: exercise
    exerciseType: write-code
    testCasesId: scaling-ex2
    difficulty: intermediate
    concept: feature-engineering-scaling
    prompt: "Write `standardize(series)`: z-score standardize a pandas Series via `(x - mean) / std` (use `.std()`'s default). Return a list of values, each rounded to 4 decimals."
  - id: quiz-1
    type: quiz
    concept: feature-engineering-scaling
    question: "Why does feature scaling matter for algorithms like gradient descent or k-nearest-neighbors, but not for a decision tree?"
    options: ["Distance- and gradient-based algorithms are sensitive to a feature's raw magnitude (a column ranging 0-1,000,000 dominates one ranging 0-1); tree splits only compare within one feature at a time, so scale doesn't matter", "Scaling always makes every model more accurate, with no exceptions", "Decision trees can't handle numeric features at all", "Scaling is purely cosmetic and never affects model behavior"]
    answer: 0
  - id: checkpoint-1
    type: checkpoint
---

<Explanation id="intro">

Features on wildly different scales (income in dollars: 20,000-500,000;
age in years: 18-90) cause problems for many models — gradient descent
(from the Optimization course) takes wildly different step sizes to
matter across such different ranges, and distance-based methods let the
larger-magnitude feature dominate purely because of its units, not its
actual importance.

**Min-max scaling** rescales every value into `[0, 1]`:

```python
import pandas as pd

ages = pd.Series([18, 35, 90])
scaled = (ages - ages.min()) / (ages.max() - ages.min())
print(scaled.tolist())   # [0.0, 0.236..., 1.0]
```

</Explanation>

<CodeExample id="try-it" language="python" packages={["pandas"]} code={`import pandas as pd\n\nprices = pd.Series([10, 50, 200, 30])\nscaled = (prices - prices.min()) / (prices.max() - prices.min())\nprint([round(v, 3) for v in scaled])`} caption="Min-max scaling is sensitive to outliers -- one extreme value stretches the whole [0,1] range, squeezing every other value toward one end." />

<Explanation id="standardization">

**Standardization** (z-score scaling) instead centers values around a
mean of 0 with a standard deviation of 1 — less sensitive to outliers
than min-max scaling, and the standard choice for algorithms that assume
roughly normally-distributed input:

```python
values = pd.Series([2, 4, 4, 4, 5, 5, 7, 9])
standardized = (values - values.mean()) / values.std()
print([round(v, 2) for v in standardized])
```

This is exactly the same z-score formula from the Statistics course's
Hypothesis Testing module — feature scaling and hypothesis testing are
using the identical underlying idea: "how many standard deviations away
from the mean is this value," just applied to a different purpose (model
input vs. statistical significance).

</Explanation>

<Exercise id="exercise-1" />

<Exercise id="exercise-2" />

<Quiz id="quiz-1" />

<Checkpoint id="checkpoint-1">
You can rescale a feature via min-max scaling (bounded [0,1]) or standardization (mean 0, std 1), and know which model types actually need this step.
</Checkpoint>
