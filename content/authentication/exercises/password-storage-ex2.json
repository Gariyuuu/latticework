{
  "id": "password-storage-ex2",
  "type": "write-code",
  "language": "python",
  "starterCode": "import hashlib\n\ndef hash_password(password, salt):\n    return hashlib.sha256((salt + password).encode()).hexdigest()\n\ndef verify_password(password, salt, stored_hash):\n    ...\n",
  "cases": [
    { "call": "verify_password('mypassword123', 'randomsalt1', hash_password('mypassword123', 'randomsalt1'))", "expect": "True" },
    { "call": "verify_password('wrongpass', 'randomsalt1', hash_password('mypassword123', 'randomsalt1'))", "expect": "False" },
    { "call": "verify_password('different', 'salt2', hash_password('different', 'salt2'))", "expect": "True" }
  ]
}
