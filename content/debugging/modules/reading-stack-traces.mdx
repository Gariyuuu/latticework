---
slug: reading-stack-traces
title: Reading Stack Traces
estimatedMinutes: 16
blocks:
  - id: intro
    type: explanation
  - id: try-it
    type: codeExample
    language: python
  - id: where-to-look
    type: explanation
  - id: exercise-1
    type: exercise
    exerciseType: write-code
    testCasesId: reading-stack-traces-ex1
    difficulty: beginner
    concept: debugging-stack-traces
    prompt: "Write `safe_first_char(s)`: return the first character of `s`, or an empty string if `s` is empty — fix the `IndexError` a naive `s[0]` would raise on empty input."
  - id: exercise-2
    type: exercise
    exerciseType: write-code
    testCasesId: reading-stack-traces-ex2
    difficulty: intermediate
    concept: debugging-stack-traces
    prompt: "Write `divide_safely(a, b)`: return `a / b`, or the string `'Error: division by zero'` if `b` is 0 — catch the specific exception (`ZeroDivisionError`) a traceback would name."
  - id: quiz-1
    type: quiz
    concept: debugging-stack-traces
    question: "When reading a Python traceback, where should you generally look FIRST to find the actual error?"
    options: ["The last line — it shows the exception type and message; then work upward through the call stack to trace where it originated", "The first line always shows the bug directly", "Tracebacks print in random order and can't be reliably read", "Only the file name matters, never the line number"]
    answer: 0
  - id: checkpoint-1
    type: checkpoint
---

<Explanation id="intro">

A Python traceback looks intimidating but has a precise structure: it
lists the chain of function calls that led to the error (oldest call
first, most recent last), and the VERY LAST LINE tells you the exception
TYPE and MESSAGE — that's almost always where to start reading.

```
Traceback (most recent call last):
  File "app.py", line 12, in <module>
    result = process(data)
  File "app.py", line 7, in process
    return data[0]
IndexError: list index out of range
```

Read bottom-up: `IndexError: list index out of range` tells you WHAT
went wrong; `File "app.py", line 7, in process` tells you WHERE.

</Explanation>

<CodeExample id="try-it" language="python" code={`def get_first(items):\n    return items[0]   # crashes with IndexError if items is empty\n\ntry:\n    get_first([])\nexcept IndexError as e:\n    print(f"Caught: {e}")   # Caught: list index out of range`} caption="Catching the exception here lets you see its message programmatically -- in a real traceback (uncaught), Python prints this same message as the last line automatically." />

<Explanation id="where-to-look">

The call chain ABOVE the last line (read bottom-to-top) shows you HOW
execution got there — useful when the error itself is generic (like
`TypeError: 'NoneType' object is not subscriptable`) and you need to
trace back through several function calls to find which one actually
returned `None` unexpectedly.

```python
def safe_first_char(s):
    if len(s) == 0:
        return ""
    return s[0]
```

Fixing the bug the traceback points at is often this simple — the hard
part is genuinely reading what the traceback is telling you (exact
exception type, exact line) instead of guessing or skimming past it.

</Explanation>

<Exercise id="exercise-1" />

<Exercise id="exercise-2" />

<Quiz id="quiz-1" />

<Checkpoint id="checkpoint-1">
You can read a traceback bottom-up (exception type/message first, then trace the call chain), and fix the specific error it identifies.
</Checkpoint>
