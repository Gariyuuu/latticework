{
  "id": "gradients-ex1",
  "type": "write-code",
  "language": "python",
  "starterCode": "def gradient(f, point, h=1e-5):\n    ...\n",
  "cases": [
    { "call": "gradient(lambda p: p[0]**2 + p[1]**2, [1, 2])", "expect": "[2.0, 4.0]" },
    { "call": "gradient(lambda p: p[0]*p[1], [3, 4])", "expect": "[4.0, 3.0]" },
    { "call": "gradient(lambda p: p[0]**2, [5, 100])", "expect": "[10.0, 0.0]" }
  ]
}
